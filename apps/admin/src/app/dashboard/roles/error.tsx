'use client';

import CustomError from "../../components/common/CustomError";

const Error = () => {
  return (
    <CustomError text="Some Error Occured while fetching roles! :(" />
  );
};

export default Error;
