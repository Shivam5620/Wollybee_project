'use client';

import CustomError from "../../components/common/CustomError";

const Error = () => {
  return (
    <CustomError text="Some Error occured while fetching product categories" />
  );
};

export default Error;
