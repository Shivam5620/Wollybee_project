'use client';
import React from 'react';
import CustomError from '../components/common/CustomError';

const Error = () => {
  return <CustomError text="Error Occured while fetching FAQ's" />;
};

export default Error;
