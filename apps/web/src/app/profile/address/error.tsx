'use client';
import React from 'react';
import CustomError from '../../components/common/CustomError';

const Error = () => {
  return <CustomError text="Error Occured while fetching addresses" />;
};

export default Error;
